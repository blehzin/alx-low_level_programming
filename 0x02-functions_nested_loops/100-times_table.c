#include "main.h"

/**
 * print_times_table - prints the n times table, starting with 0
 * @n: a number of the times table
 */

void print_times_table(int n)
{
	int i;
	int sum;

	sum = 0;

	for (i = 0; i < 1024; i++)
	{
		{
			for (j = 0; j <= n; j++)
		
				_putchar(k + '0');
			} else if (k < 10 && j != 0)
			{
				_putchar(',');
					_putchar(' ');
					_putchar(' ');
					_putchar(' ');
					_putchar(k + '0');
			} else if (k >= 10 && k < 100)
			{
				_put
		_putchar('\n');
	}
}
